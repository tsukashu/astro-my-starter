---
import "@/styles/styles.scss";

const { title = "title", description = "description", noindex = true } = Astro.props;
---

<!-- headの中身、global CSSとかScriptの読み込み、bodyの骨格を提供するレイアウトコンポーネント --><!-- これも余計なことはしない -->
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex" />}
  </head>
  <body>
    <slot />
  </body>
</html>
